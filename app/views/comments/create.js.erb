comment = document.getElementById("comments-for-<%= "#{@comment.commentable_type.downcase}-#{@commentable.id}" %>")
comment.innerHTML = "<%= j render(template: 'comments/index', locals: {commentable: @commentable}) %>"
